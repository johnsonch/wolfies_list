<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <ul>
    <% @category.ads.each do |ad| %>
    <li><%= link_to ad.title, ad_path(ad) %>, $<%= ad.price %></li>
    <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
